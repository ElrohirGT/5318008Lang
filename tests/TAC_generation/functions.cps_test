function sumar(n: integer): integer {
	if (n <= 0) {
		return 0;
	}

	return n + sumar(n-1);
}
---

SEC sumar:
	LOAD t1

	IF LTE t1 0 GOTO sumar_return_zero

	= t2 i32 n
	SUB t2 t2 1
	PARAM t2
	CALLRET t2 sumar 1

	ADD t3 t1 t2
	RETURN t3

	SEC sumar_return_zero:
		RETURN 0
