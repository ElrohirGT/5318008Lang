class Animal {
  let name: string;

  function constructor(name: string) {
    function addSuffix(p1: string): string {
      return p1 + "ending";
    }
    this.name = addSuffix(name);
  }

	function say(something: string): string {
		return something;
	}
}

class Dog : Animal {
  
}

let a = new Dog("Byte");

// THIS SHOULD FAIL!
let b = a.speak();
// This should not fail!
a.say("Hola!");

class Cat : Animal {
	function speak(): string {
		return "Hello!";
	}
}

let c = new Cat("Felix");
// This should not fail!
c.speak();
c.say("Blablabla");
---
=== ERRORS ===
* Error: (line: 23, column: 9-16) Can't call undefined method `speak` on type `Dog`
