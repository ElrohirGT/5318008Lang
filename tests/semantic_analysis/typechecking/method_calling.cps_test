class Cell {
	let row: string = "";
	let column: string = "";

	function constructor(row: string, column: string) {
		this.row = row;
		this.column = column;
	}

	function setRow(row: string) {
		this.row = row;
	}

	function setColumn(column: integer) {
		this.column = column;
	}
}

let cell = new Cell("5", "10");
cell.setRow("20");
cell.setRow(20);

let cell2 = new Cell(1, 10);
cell2.setRow("10");
cell2.setColumn(10);
---
=== ERRORS ===
* Error: (line: 15, column: 7-22) Trying to assign `column` to variable `column` but types don't match! (`string` != `integer`)
* Error: (line: 23, column: 21-22) Constructor for `Cell` requires `1` to be of type `string` but it's `integer` instead!
* Error: (line: 23, column: 24-26) Constructor for `Cell` requires `10` to be of type `string` but it's `integer` instead!
