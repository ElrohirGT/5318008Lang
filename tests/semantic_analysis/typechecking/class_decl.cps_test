class Animal {
	let name;
	let age;

	function speak(): string {
		let animalVar = new Animal();
		return this.name + " makes a sound.";
	}

	function setAge(newAge: integer) {
		this.age = 5;
	}

	function printAge(): string {
		return this.name + " is " + this.age +" years old!";
	}

}

let animalVar = new Animal();
---
=== ERRORS ===
* Error: (line: 6) Can't create a new instance of `Animal` while defining the same class!
* Error: (line: 6) Exit Add (firstExpr): `newAnimal()` doesn't have a type!
* Error: (line: 6) Couldn't infer the type of variable `animalVar`, initialized with: `newAnimal()`
* Error: (line: 15) Can't add:
 * leftSide: `this.name + " is "` of type `string`
 * rightSide: `this.age` of type `integer`
